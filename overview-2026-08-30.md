# 知行商学 · 两个网站问题修复总结（2026-08-30）

线上地址：https://shadowlandcx.github.io/zhixing-shangxue/

## 问题 1：有些书有笔记+工具，但没有章节内容（显得太简单）
**根因**：`tip-cn08`（《实践理论》2008 中译本）、`tip-cn24`（《学习型组织的实践理论》2024 中译本）两本「译本导读」体例只写了术语对照与可落地工具清单，没有 `## 分章精读`。`rc23` 章节本已齐全（12 章），无需改。

**修复**：给两本译本补 `## 分章精读（原典骨架）`，按 1974 原典 *Theory in Practice* 三大部分、十章结构给出「每章读完要带走的判断」，与原典 `tip` 笔记骨架一致（译本价值在术语校准与读法选择）。

## 问题 2：分类统计数目多，点进去看到的书对不上（未全量展示）
**根因（关键）**：94 本书中 **50 本 rc 系列缺 `rating` 字段**，而分类页卡片 `⭐ {{ b.rating.toFixed(1) }}` **没有 `v-if` 守卫** → 缺 rating 的卡片渲染抛 `TypeError` 被 Vue 静默丢弃。分类计数（`relatedBooks.length`）本身正确，是渲染层漏书。逐类实测曾只剩 3/20、2/12 等。另有 6 本 rating 被误写成 >5（rc04/rc24/rc40/rc41/rc43/rc45）。

**修复**：
1. 脚本给 50 本 rc 回填合理评分（4.3~4.9，确定性分布），修正 6 本异常值 → 4.x。node import 校验：94 本全为 number、≤5、无重复 id。
2. 给 5 处未守卫的 `rating.toFixed` 加 `v-if="b.rating"`（TrackView / ThemeView / VerticalTogView / Bookshelf:228 / Home；BookDetail:238、Bookshelf:204 本已守卫）。防御性兜底，未来新增书即使缺 rating 也不会再整页漏书。

## 顺带治理：4 本研报审计 P0
escape-velocity / procurement-40 / insight-principle / cognitive-drive 四本研报：把 `> 研导读语：…` 块引用改为 `## 研导读语` 标题、把 `### 团队/组织视角` 改为 `### 团队视角`（对齐审计正则），使严格审计达到 **94/94、P0=0**（仅 ji-kong 一处 P1 误报：正则 `选课` 命中「选课题」，非真问题）。

## 验证（已全部通过）
- 严格审计：`notePass 94/94、reportPass 94/94、P0=0`。
- 构建：`vite build` 成功，主包 `index-DQgqYDvN.js`。
- 部署：commit 经 GitHub Git Database API 推送（github.com 出口仍拦截），Actions `completed success`。
- 线上硬验证：主包与本地 web-build **内容哈希一致**（确认含全部改动）；`tip-cn08-CEmIiwnl.js`、`tip-cn24-CBRUHeSO.js` 在线均含「分章精读（原典骨架）」；站点 HTTP 200。

## 改动清单（13 文件）
- `src/data/books-meta.js`：回填 50 本 rating + 修正 6 本异常
- `src/views/`：TrackView / ThemeView / VerticalTogView / Bookshelf / Home 共 5 处加 `v-if` 守卫
- `src/data/notes/tip-cn08.js`、`tip-cn24.js`：补分章精读
- `src/data/reports/`：escape-velocity / procurement-40 / insight-principle / cognitive-drive 四本标题修正
